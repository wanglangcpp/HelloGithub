namespace Genesis.GameClient
{
    public class NpcBubble : BaseBubble
    {
        public override void RefreshBubble(TargetableObject targetableObject, string content, float startTime, float keepTime)
        {
            base.RefreshBubble(targetableObject, content, startTime, keepTime);
        }
    }
}
