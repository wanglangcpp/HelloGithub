//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCLeaveCosmosCrackInstanceHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2402); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCLeaveCosmosCrackInstance);
        }

        public static void Handle(object sender, LCLeaveCosmosCrackInstance response)
        {
            if (response.PlayerInfo != null)
            {
                GameEntry.Data.Player.UpdateData(response.PlayerInfo);
            }
            if (response.ReceivedItems != null)
            {
                GeneralItemUtility.UpdateItemsData(response.ReceivedItems.ItemInfo);
                GameEntry.Data.Gears.AddData(response.ReceivedItems.GearInfo);
                GameEntry.Data.Souls.AddData(response.ReceivedItems.SoulInfo);
            }

            GameEntry.Event.Fire(sender, new PlayerDataChangedEventArgs());
            GameEntry.Event.Fire(sender, new ItemDataChangedEventArgs());
            GameEntry.Event.Fire(sender, new HeroQualityItemDataChangeEventArgs());
            GameEntry.Event.Fire(sender, new LeaveInstanceResponseEventArgs());
        }
    }
}
