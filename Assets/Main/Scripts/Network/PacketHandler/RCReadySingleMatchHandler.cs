//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class RCReadySingleMatchHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.RoomServerToClient, 5009); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as RCReadySingleMatch);
        }

        public static void Handle(object sender, RCReadySingleMatch response)
        {
            // TODO: Replace code here.
            //GameEntry.Event.Fire(sender, new RoomDataChangedEventArgs());
            //GameEntry.Event.Fire(sender, new RoomReadyEventArgs());
            //GameEntry.Event.Fire(sender,new)
        }
    }
}
