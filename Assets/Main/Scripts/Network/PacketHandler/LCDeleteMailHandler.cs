//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCDeleteMailHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 3202); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCDeleteMail);
        }

        public static void Handle(object sender, LCDeleteMail response)
        {
            for (int i = 0; i < response.MailIds.Count; i++)
                GameEntry.Data.Mails.RemoveMail(response.MailIds[i]);

            if (response.MailIds.Count > 0)
                GameEntry.Event.Fire(sender, new MailDataChangedEventArgs());

            GameEntry.Event.Fire(sender, new ReminderUpdatedEventArgs());
        }
    }
}
