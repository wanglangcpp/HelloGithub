//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCClaimRewardsForTowerHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2622); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCClaimRewardsForTower);
        }

        public static void Handle(object sender, LCClaimRewardsForTower response)
        {
            GameEntry.Data.StromTowerData.UpdateData(response.InstanceForTowerInfo);
            GameEntry.Event.Fire(sender, new StromTowerInfoEventArgs(response.InstanceForTowerInfo));
        }
    }
}
