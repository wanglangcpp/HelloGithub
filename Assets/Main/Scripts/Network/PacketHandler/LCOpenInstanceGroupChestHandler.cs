//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;
using System.Collections.Generic;

namespace Genesis.GameClient
{
    public class LCOpenInstanceGroupChestHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1007); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCOpenInstanceGroupChest);
        }

        public static void Handle(object sender, LCOpenInstanceGroupChest response)
        {
            var helper = new RewardCollectionHelper();
            helper.AddCompoundItems(response.CompoundItemInfo);
            GameEntry.Data.InstanceGroups.SetChestStatus(response.InstanceGroupId, response.ChestIndex, true);
            GameEntry.Event.Fire(sender, new PlayerDataChangedEventArgs());
            var eventData = new OpenInstanceGroupChestEventArgs();
            eventData.RewardShowHelper = helper;
            eventData.ChapterId = response.InstanceGroupId;
            eventData.ChestIndex = response.ChestIndex;
            GameEntry.Event.Fire(sender, eventData);
        }
    }
}
