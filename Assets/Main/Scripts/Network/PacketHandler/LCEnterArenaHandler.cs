//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCEnterArenaHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2302); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCEnterArena);
        }

        public static void Handle(object sender, LCEnterArena response)
        {
            GameEntry.Data.OfflineArena.RefreshChallangeCount(response.PlayArenaCount);
            GameEntry.Event.Fire(sender, new OfflineArenaDataChangedEventArgs());

            if (GameEntry.Procedure.CurrentProcedure is ProcedureMain)
            {
                GameEntry.Data.AddOrUpdateTempData(Constant.TempData.AutoOpenOfflineArena, true);
                GameEntry.Event.Fire(sender, new WillChangeSceneEventArgs(InstanceLogicType.ArenaBattle, Constant.DefaultPlayerVSPlayerAIInstanceId, true));
            }
        }
    }
}
