//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCLevelUpSkillBadgeHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1408); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCLevelUpSkillBadge);
        }

        public static void Handle(object sender, LCLevelUpSkillBadge response)
        {
            if (response.HeroInfo != null)
            {
                var data = GameEntry.Data.LobbyHeros.GetData(response.HeroInfo.Type);
                if (data != null)
                {
                    data.UpdateData(response.HeroInfo);
                }
            }

            GameEntry.Data.Player.UpdateData(response.PlayerInfo);

            GeneralItemUtility.UpdateItemsData(response.BadgeItemsData);

            GameEntry.Event.Fire(sender, new LobbyHeroDataChangedEventArgs());
            GameEntry.Event.Fire(sender, new HeroSkillDataChangedEventArgs(HeroSkillDataChangedEventArgs.SkillChangeType.SkillLevelUp));
            GameEntry.Event.Fire(sender, new PlayerDataChangedEventArgs());
            GameEntry.Event.Fire(sender, new OnSkillBadgeDataChangeEventArgs());
            GameEntry.Event.Fire(sender, new OnSkillBadgeBagDataChangedEventArgs());
        }
    }
}
