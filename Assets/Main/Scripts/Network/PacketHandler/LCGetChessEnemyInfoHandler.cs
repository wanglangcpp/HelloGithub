//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCGetChessEnemyInfoHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2004); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            LCGetChessEnemyInfo response = packet as LCGetChessEnemyInfo;
            if (response.Success)
            {
                GameEntry.Data.ChessBattleEnemy.UpdateData(response);
                GameEntry.Event.Fire(this, new ChessBoardGetEnemyDataSuccessEventArgs(response.ChessFieldIndex));
            }
            else
            {
                GameEntry.Event.Fire(this, new ChessBoardGetEnemyDataFailureEventArgs(response.ChessFieldIndex));
            }
        }
    }
}
