//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCOpenChessFieldHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2002); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            LCOpenChessField response = packet as LCOpenChessField;

            if (response.Success)
            {
                if (response.PlayerInfo != null)
                {
                    GameEntry.Data.Player.UpdateData(response.PlayerInfo);
                    GameEntry.Event.Fire(this, new PlayerDataChangedEventArgs());
                }

                GameEntry.Data.ChessBattleMe.UpdateData(response.MyHeroesStatus);
                GameEntry.Event.Fire(this, new ChessBoardOpenChessFieldSuccessEventArgs(response));
            }
            else
            {
                GameEntry.Event.Fire(this, new ChessBoardOpenChessFieldFailureEventArgs());
            }
        }
    }
}
