//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;
using UnityEngine;

namespace Genesis.GameClient
{
    public class LCEnterCosmosCrackInstanceHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2401); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCEnterCosmosCrackInstance);
        }

        public static void Handle(object sender, LCEnterCosmosCrackInstance response)
        {
            if (GameEntry.Procedure.CurrentProcedure is ProcedureMain)
            {
                GameEntry.Event.Fire(sender, new WillChangeSceneEventArgs(InstanceLogicType.CosmosCrack, response.InstanceType, true,
                    new CosmosCrackInstanceLogic.InitUserData
                    {
                        OverwritingNpcLevel = Mathf.Max(response.NpcLevel, 1),
                    }));
            }
        }
    }
}
