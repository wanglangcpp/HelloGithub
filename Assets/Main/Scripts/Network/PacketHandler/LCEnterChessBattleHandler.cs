//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCEnterChessBattleHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2005); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            LCEnterChessBattle response = packet as LCEnterChessBattle;

            if (response.HeroTeam != null)
            {
                GameEntry.Data.ChessBattleMe.UpdateData(response.HeroTeam);
                GameEntry.Event.Fire(this, new HeroTeamDataChangedChessBattleEventArgs());
            }

            if (GameEntry.Procedure.CurrentProcedure is ProcedureMain)
            {
                GameEntry.Event.Fire(this, new WillChangeSceneEventArgs(InstanceLogicType.ChessBattle, Constant.DefaultPlayerVSPlayerAIInstanceId, true));
            }
        }
    }
}
