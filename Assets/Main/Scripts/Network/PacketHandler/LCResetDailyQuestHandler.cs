//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCResetDailyQuestHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1060); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCResetDailyQuest);
        }

        public static void Handle(object sender, LCResetDailyQuest response)
        {
            GameEntry.Data.Player.ActivenessToken = 0;
            var datas = GameEntry.Data.DailyQuests.Data;
            for (int i = 0; i < datas.Count; i++)
            {
                datas[i].ResetDailyQuest();
            }
            GameEntry.Event.Fire(sender, new DailyQuestsRefreshedEventArgs());
        }
    }
}
