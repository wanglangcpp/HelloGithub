//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCClaimDailyLoginHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1071); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCClaimDailyLogin);
        }

        public static void Handle(object sender, LCClaimDailyLogin response)
        {
            //GameEntry.Data.DailyLogin.UpdateData(response.DailyLoginInfo.ClaimCount, response.DailyLoginInfo.CanClaim);
            GameEntry.Data.EveryDaySignInData.UpdataData(response.DailyLoginInfo);

            var helper = new RewardCollectionHelper();
            helper.AddCompoundItems(response.CompoundItemInfo);
            GameEntry.Event.Fire(sender, new DailyLoginedEventArgs(helper));
            GameEntry.Event.Fire(sender, new ItemDataChangedEventArgs());
            GameEntry.Event.Fire(sender, new PlayerDataChangedEventArgs());
        }
    }
}
