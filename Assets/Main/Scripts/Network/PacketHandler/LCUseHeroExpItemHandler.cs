//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCUseHeroExpItemHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1033); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCUseHeroExpItem);
        }

        public static void Handle(object sender, LCUseHeroExpItem response)
        {
            var heroes = GameEntry.Data.LobbyHeros;
            heroes.GetData(response.HeroInfo.Type).UpdateData(response.HeroInfo);
            GameEntry.Event.Fire(sender, new LobbyHeroDataChangedEventArgs());

            GeneralItemUtility.UpdateItemsData(response.ItemInfo);
            GameEntry.Event.Fire(sender, new ItemDataChangedEventArgs());

            GameEntry.Event.Fire(sender, new UseHeroExpItemEventArgs());

            GameEntry.Data.Player.UpdateData(response.PlayerInfo);
            GameEntry.Event.Fire(sender, new PlayerDataChangedEventArgs());
        }
    }
}
