//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCGetPlayerEnergyHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1038); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCGetPlayerEnergy);
        }

        public static void Handle(object sender, LCGetPlayerEnergy response)
        {
            GameFramework.Log.Info("=================NextEnergyRecoveryTime " + ((long)response.PlayerInfo.NextEnergyRecoveryTime).ToString() + " " + System.DateTime.Now.Ticks + " " + System.DateTime.UtcNow.Ticks + " " + GameEntry.Time.LobbyServerUtcTime.Ticks + " // " + response.PlayerInfo.Energy);//这里收到的时间有问题，都是初始时间的

            GameEntry.Data.Player.UpdateData(response.PlayerInfo);
            GameEntry.Event.Fire(sender, new PlayerDataChangedEventArgs());
            GameEntry.Event.Fire(sender, new GotPlayerEnergyEventArgs());
        }
    }
}
