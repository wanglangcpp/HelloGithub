//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCUpdateAchievementHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1051); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCUpdateAchievement);
        }

        public static void Handle(object sender, LCUpdateAchievement response)
        {
            for (int i = 0; i < response.AchievementInfo.Count; i++)
            {
                var data = response.AchievementInfo[i];
                var achievementData = GameEntry.Data.Achievements.GetData(data.AchievementId);
                if (achievementData != null)
                {
                    achievementData.UpdateData(data);
                }
                else
                {
                    GameEntry.Data.Achievements.AddData(data);
                }
            }

            GameEntry.Event.Fire(sender, new UpdateAchievementEventArgs());
        }
    }
}
