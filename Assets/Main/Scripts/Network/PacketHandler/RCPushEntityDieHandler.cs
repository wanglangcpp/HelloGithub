//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework;
using GameFramework.Network;

namespace Genesis.GameClient
{
    public class RCPushEntityDieHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.RoomServerToClient, 4110); } }

        public override void Handle(object sender, Packet packet)
        {
            if (GameEntry.Data.Room.HasReconnected)
            {
                return;
            }
            base.Handle(sender, packet);
            Handle(sender, packet as RCPushEntityDie);
        }

        public static void Handle(object sender, RCPushEntityDie response)
        {
            if (GameEntry.SceneLogic.SinglePvpInstanceLogic == null)
            {
                Log.Info("The client is not in a single PVP instance.");
                return;
            }
            if (GameEntry.SceneLogic.SinglePvpInstanceLogic.PlayerIsMe(response.PlayerId))
                Log.Debug("=====================>True");
            else
                Log.Debug("=====================>False");
            GameEntry.SceneLogic.SinglePvpInstanceLogic.ApplyGoDie(response.PlayerId, response.DeadEntityId);
        }
    }
}
