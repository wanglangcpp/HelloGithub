//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework;
using GameFramework.Event;
using GameFramework.Network;

namespace Genesis.GameClient
{
    public class RCGetRoomBattleStatusHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.RoomServerToClient, 5014); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as RCGetRoomBattleStatus);
        }

        public static void Handle(object sender, RCGetRoomBattleStatus response)
        {
            Log.Debug("RCGetRoomBattleStatusHandler ");
            GameEntry.Event.Fire(sender, new GetRoomBattleStatusEventArgs(response));
        }
    }

    public class GetRoomBattleStatusEventArgs : GameEventArgs
    {
        public RCGetRoomBattleStatus BattleStatus { private set; get; }
        public GetRoomBattleStatusEventArgs(RCGetRoomBattleStatus response)
        {
            BattleStatus = response;
        }

        public override int Id
        {
            get
            {
                return (int)EventId.GetRoomBattleStatus;
            }
        }
    }
}

