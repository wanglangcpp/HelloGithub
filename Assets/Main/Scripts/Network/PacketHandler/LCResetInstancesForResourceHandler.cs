//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCResetInstancesForResourceHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2602); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCResetInstancesForResource);
        }

        public static void Handle(object sender, LCResetInstancesForResource response)
        {
            if (response.InstanceForCoinResource != null)
            {
                GameEntry.Data.InstanceForCoinResourceData.UpdateData(response.InstanceForCoinResource);
            }

            if (response.InstanceForExpResource != null)
            {
                GameEntry.Data.InstanceForExpResourceData.UpdateData(response.InstanceForExpResource);
            }

            GameEntry.Event.Fire(sender, new InstancesForResourceDataChangedEventArgs());
        }
    }
}
