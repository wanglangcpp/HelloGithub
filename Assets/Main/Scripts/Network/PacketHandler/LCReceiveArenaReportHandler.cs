//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCReceiveArenaReportHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 2305); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCReceiveArenaReport);
        }

        public static void Handle(object sender, LCReceiveArenaReport response)
        {
            if (response.HasMyRank)
            {
                GameEntry.Data.OfflineArena.RefreshRank(response.MyRank);
            }
            GameEntry.Data.OfflineArenaBattleReports.AddData(response.ArenaReportInfo);
            GameEntry.Event.Fire(sender, new OfflineArenaReportListChangedEventArgs());
        }
    }
}
