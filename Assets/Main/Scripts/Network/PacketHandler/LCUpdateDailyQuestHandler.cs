//----------------------------------------------------------------------------------------------------
// This code was auto generated by tools.
// You may need to modify 'Handle' method.
// Don't modify the rest unless you know what you're doing.
//----------------------------------------------------------------------------------------------------

using GameFramework.Network;

namespace Genesis.GameClient
{
    public class LCUpdateDailyQuestHandler : BasePacketHandler
    {
        public override int OpCode { get { return NetworkHelper.GetOpCode(PacketType.LobbyServerToClient, 1061); } }

        public override void Handle(object sender, Packet packet)
        {
            base.Handle(sender, packet);
            Handle(sender, packet as LCUpdateDailyQuest);
        }

        public static void Handle(object sender, LCUpdateDailyQuest response)
        {
            for (int i = 0; i < response.DailyQuestInfo.Count; i++)
            {
                var data = response.DailyQuestInfo[i];
                var dailyQuestsData = GameEntry.Data.DailyQuests.GetData(data.DailyQuestId);
                if (dailyQuestsData != null)
                {
                    dailyQuestsData.UpdateData(data);
                }
                else
                {
                    GameEntry.Data.DailyQuests.AddData(data);
                }
            }

            GameEntry.Event.Fire(sender, new UpdateDailyQuestEventArgs());
        }
    }
}
